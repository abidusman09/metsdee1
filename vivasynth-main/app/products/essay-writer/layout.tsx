import React from "react";

export const metadata = {
  title: "Essay Writer",
  description: `Unlock the Power of Language-Based generative AI for Your Essays`,
};

const Layout = ({ children }: { children: React.ReactNode }) => {
  return children;
};

export default Layout;
